---
import { resume } from "../../data/resume";
import { useTranslate } from "../../i18n/utils";
import ProjectCard from "../ProjectCard.astro";

const projects = resume.projects;

const t = useTranslate(Astro.currentLocale);
---

<div class="mx-6 sm:mx-0 mt-10 gap-4 flex flex-col">
  <h1 class="text-2xl font-bold text-amber-200/90">
    {t("MY_PROJECTS")}
  </h1>
  <h2 class="mb-6">
    {t("MY_PROJECTS_CARD_DESCRIPTION")}
  </h2>
</div>

<div
  id="projects"
  class="flex gap-5 overflow-x-auto w-full px-6 lg:px-0 rounded-2xl overflow-hidden md:bg-neutral-700/10 p-4 border-neutral-400/10 md:border"
>
  {projects.map((project) => <ProjectCard project={project} />)}
</div>

<style>
  #projects {
    scrollbar-width: none;
  }
</style>
